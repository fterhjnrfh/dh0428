# 项目工程原则

## 跨平台编译与性能优先原则

本项目需要兼容不同操作系统和运行环境，因此应在构建体系中提供明确的编译项选择机制。不同系统下编译时，应能够自动识别目标平台，并按平台加载对应的原生库、运行时组件和依赖实现。

平台相关库的选型应遵循“适配性优先、性能优先、工程稳定性优先”的原则：在每个目标系统下，优先采用最适合本项目架构、性能表现最佳、维护成熟且与当前技术栈兼容度最高的库。不得为了形式上的统一而牺牲目标平台上的运行效率、稳定性或可维护性。

性能优化的总体方向是以空间换时间。在关键数据采集、解析、缓存、显示和存储链路中，可以接受更高的内存占用，以换取更低的延迟、更高的吞吐和更稳定的实时表现。实现时应优先考虑多级缓存、预分配、对象复用、批处理、局部性优化和减少重复计算等策略。

凡具备 GPU 加速条件的计算、渲染、编解码、数据转换和可并行处理环节，应优先采用 GPU 方案。GPU 路径应作为首选实现纳入设计和验证范围，CPU 路径仅作为兼容、回退或小规模数据场景下的备用方案。实现时应综合评估目标平台的图形 API、计算 API、驱动成熟度和数据传输成本，确保 GPU 加速真正服务于低延迟、高吞吐和实时稳定性目标。

跨平台实现中，应将平台差异隔离在清晰的边界内，使业务层保持稳定一致。平台选择、库加载和性能策略应尽量配置化、可观测、可验证，避免将平台判断散落在业务逻辑中。
