<UserControl x:Class="Example_Demo.ShowDataView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Example_Demo"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance local:ShowDataModel}"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.Resources>
            <Style TargetType="TextBlock">
                <Setter Property="HorizontalAlignment" Value="Left"></Setter>
                <Setter Property="VerticalAlignment" Value="Center"></Setter>
            </Style>
        </Grid.Resources>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="20"/>
            <RowDefinition/>
            <RowDefinition Height="20"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <StackPanel HorizontalAlignment="Left" Margin="5" Orientation="Horizontal">
            <Button Content="清空面板" Width="60" Click="btnClear_Click"></Button>
            <Button Content="启动刷新" Width="60" Click="btnStart_Click"></Button>
            <Button Content="暂停刷新" Width="60" Click="btnStop_Click"></Button>
            <TextBlock Text="(数据仅展示一批数据中的第一个点)" Grid.Row="1" Grid.Column="0" VerticalAlignment="Center"></TextBlock>
        </StackPanel>
        <TextBlock Text="时域数据" Grid.Row="1" Grid.Column="0"></TextBlock>
        <TextBox HorizontalAlignment="Stretch" VerticalAlignment="Stretch" TextWrapping="Wrap" ScrollViewer.VerticalScrollBarVisibility="Visible" 
                 Text="{Binding Data}" Margin="5" Grid.Row="2" Grid.RowSpan="3" Height="Auto" Width="Auto">
        </TextBox>

        <TextBlock Text="485/计数器数据" Grid.Row="1" Grid.Column="1"></TextBlock>
        <TextBox HorizontalAlignment="Stretch" VerticalAlignment="Stretch" TextWrapping="Wrap" ScrollViewer.VerticalScrollBarVisibility="Visible" 
                 Text="{Binding StatData}" Margin="5" Grid.Row="2" Grid.Column="1" Height="Auto" Width="Auto">
        </TextBox>
        <StackPanel Grid.Row="3" Grid.Column="1" Orientation="Horizontal">
            <TextBlock Text="GPS数据"></TextBlock>
            <ComboBox Width="150" Margin="5 0 0 0" ItemsSource="{Binding Devices}" 
                      SelectedItem="{Binding SelectDevice}" DisplayMemberPath="DeviceInfo"></ComboBox>
            <Button Content="获取数据" Click="btnGetGPSData_Click" Margin="5 0 0 0"></Button>
        </StackPanel>
        <TextBox HorizontalAlignment="Stretch" VerticalAlignment="Stretch" TextWrapping="Wrap" ScrollViewer.VerticalScrollBarVisibility="Visible" 
                 Text="{Binding GpsData}" Margin="5" Grid.Row="4" Grid.Column="1" Height="Auto" Width="Auto">
        </TextBox>
    </Grid>
</UserControl>
