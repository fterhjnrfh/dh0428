<Window x:Class="Example_Demo.Source.View.OutDataSetWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Example_Demo.Source.View"
        mc:Ignorable="d"
        Title="外部数据源设置" Height="300" Width="450">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="30"/>
        </Grid.RowDefinitions>
        <ScrollViewer>
            <ItemsControl x:Name="itemsOutDataSource" Margin="5">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="90"/>
                                <ColumnDefinition Width="50"/>
                                <ColumnDefinition/>
                                <ColumnDefinition/>
                                <ColumnDefinition Width="50"/>
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="外部数据源类型" VerticalAlignment="Center"></TextBlock>
                            <TextBox Text="{Binding OutDataType}" IsEnabled="False" Grid.Column="1"></TextBox>
                            <CheckBox Content="是否启用" IsChecked="{Binding UseOutDataSource}" Grid.Column="2" VerticalAlignment="Center" HorizontalAlignment="Center"></CheckBox>
                            <TextBox Text="{Binding StrIP}" Grid.Column="3"></TextBox>
                            <TextBox Text="{Binding PortID}" Grid.Column="4"></TextBox>
                        </Grid>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
        <StackPanel Grid.Row="1" HorizontalAlignment="Center" Orientation="Horizontal">
            <Button x:Name="btnSetup" Content="设置" Click="btnSetup_Click"></Button>
            <Button x:Name="btnClose" Content="关闭" Click="btnClose_Click"></Button>
        </StackPanel>
    </Grid>
</Window>
